# Core API. Ranges rather than pins so the stack installs on Python 3.11-3.13.
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
httpx>=0.27,<1.0

# Data pipeline and analytics
pandas>=2.2,<4.0
numpy>=1.26,<3.0
scipy>=1.11,<2.0

# Flood risk model (Level 2). shapely is only used by backend/flood_pipeline.py.
scikit-learn>=1.4,<2.0
shapely>=2.0,<3.0

# Verifies TLS against the OS trust store (TLS-inspecting proxies); optional.
truststore>=0.9,<1.0
